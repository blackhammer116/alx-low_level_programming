#include <stdio.h>
/**
 *Description: main - iterates through the alphabet and printing each one.
 *
 *Return: 0 if functional
 */
int main(void)

{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch"\n");
	}
return 0;
}
