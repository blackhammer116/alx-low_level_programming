#include <stdio.h>
/**
 *Description: main will print a string onto stdout using printf.
 *
 * Return: 0 if functional.
 */
int main(void);
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
