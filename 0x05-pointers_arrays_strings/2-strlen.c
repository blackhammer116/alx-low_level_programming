#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - calculates the number of characters
 * a string has
 * @s: is the string parameter to be passed
 *Return: strlen(s)
 */
int _strlen(char *s)
{
	strlen(s);
	return (strlen(s));
}
