#include "main.h"
#include <string.h>
/**
 *_strcmp - compares the two strings
 * @s1: first parameter
 * @s2: second parameter
 * Return: returns the int value of var
 */
int _strcmp(char *s1, char *s2)
{
	int var = strcmp(s1, s2);

	return (var);
}
