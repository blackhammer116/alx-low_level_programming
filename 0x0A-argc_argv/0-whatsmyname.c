#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the program name
 * @argc: argument count
 * @argv: pointer to an array of characters
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
